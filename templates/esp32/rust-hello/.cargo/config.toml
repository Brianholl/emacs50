[target.xtensa-esp32s3-none-elf]
runner = "espflash flash --monitor --chip esp32s3"

[env]
ESP_LOG = "info"

[build]
target = "xtensa-esp32s3-none-elf"
# esp-hal 1.0: linkall.x lo inyecta build.rs — no duplicar acá.
rustflags = []

[unstable]
build-std = ["core"]
